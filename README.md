# MINI-PROJECT
The application provides an administrator dashboard to manage departments, faculty, subjects, classrooms, and time slots. Using predefined scheduling constraints, the AI module generates optimized timetables that prevent faculty clashes, classroom conflicts, and overlapping schedules.
